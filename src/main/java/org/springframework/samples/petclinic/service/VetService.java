package org.springframework.samples.petclinic.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.samples.petclinic.vet.repository.VetRepository;
import org.springframework.samples.petclinic.model.Vets;

public class VetService {

	/*
	 * @Autowired VetRepository vetRepository;
	 *
	 * @org.springframework.beans.factory.annotation.Autowired(required=true) public Vets
	 * showResourcesVetList() { Vets vets = new Vets();
	 * vets.getVetList().addAll(vetRepository.findAll()); return vets; }
	 */

}
